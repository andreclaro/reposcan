# This is a template - DO NOT commit real values!
# Create secret with: kubectl create secret generic app-secrets --from-env-file=.env -n secaudit
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: secaudit
type: Opaque
stringData:
  # Replace with actual values:
  DATABASE_URL: "postgresql://postgres:CHANGEME@postgres:5432/sec_audit"
  POSTGRES_PASSWORD: "CHANGEME"
  # AI Provider (optional)
  AI_ANALYSIS_ENABLED: "false"
  AI_PROVIDER: "anthropic"
  ANTHROPIC_API_KEY: ""
  # Add other secrets as needed
